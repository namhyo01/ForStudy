#include <bits/stdc++.h>
using namespace std;
/*
int A[10001];

int main() {
	ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	int N;
	cin >> N;
	for (int i = 0; i < N; i++)
		cin >> A[i];
	for(int i=0)
	system("pause");
	return 0;
}
*/