#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <string>
#include<math.h>
#include <memory.h>
#include <stack>
#include <queue>
using namespace std;
/*
int N;
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	cin >> N;
	queue<int> aa;
	for (int i = 1; i <= N; i++)
		aa.push(i);
	while (1) {
		if (aa.size() == 1)
		{
			cout << aa.front() << endl;
			break;
		}
		aa.pop();
		if (aa.size() == 1)
		{
			cout << aa.front() << endl;
			break;
		}
		int temp = aa.front();
		aa.push(temp);
		aa.pop();

	}
	system("pause");
	return 0;

}
*/