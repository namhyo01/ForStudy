#include <bits/stdc++.h>

using namespace std;
/*
bool check_right(string id) {
	int length = id.size();
	if (3> length && length > 15) {
		return false;
	}
	for (int i = 0; i < id.size(); i++) {
		if ((id[i] >= '0'&&id[i] <= '9') || (id[i] >= 'a'&&id[i] <= 'z') || id[i] == '-' || id[i] == '_' || id[i] == '.') {
			if ((i == 0 || i==id.size()-1) && id[i] == '.') {
				return false;
			}
			if (id[i] == '.' && id[i - 1] == '.')
				return false;
			continue;
		}
		else {
			return false;
		}
	}
	return true;
}




string solution(string new_id) {
	string answer = "";
	return answer;
}

int main() {
	ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	string p;
	cin >> p;
	solution(p);

	system("pause");
	return 0;
}*/