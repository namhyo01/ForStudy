#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cmath>
#include <set>
#pragma warning(disable:4996)
using namespace std;
/*
int n;

int main() {
	int num_5 = 0, num_2=0;
	scanf("%d", &n);
	for (int i = 2; i <= n; i++) {
		int temp = i;
		while (temp>1) {
			if (temp % 2 == 0) {
				num_2++;
				temp = temp/2;
			}
			else if (temp % 5 == 0) {
				num_5++;
				temp = temp/5;
			}
			else
				break;
		}
	}
	int min = num_5 > num_2 ? num_2 : num_5;
	printf("%d\n", min);
	system("pause");
	return 0;
}*/