#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <string>
#include<math.h>
#include <memory.h>
using namespace std;
/*
int T;
int A, B;
int gcd(int a, int b) {
	return b ? gcd(b, a%b) : a;
}
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	cin >> T;
	while (T--) {
		cin >> A >> B;
		int G = gcd(A, B);
		int result = (A / G)*(B / G)*G;
		cout << result << endl;
	}
	system("pause");
	return 0;
}*/