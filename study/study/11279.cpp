#include <iostream>
#include <algorithm>
#include <climits>
#include <functional>
#include <vector>
#include <string>
#include <string.h>
#include<math.h>
#include <stdio.h>
#include <memory.h>
#include <stack>
#include <queue>
#include <unordered_map>
#include <deque>
using namespace std;
/*
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	int N;
	priority_queue<int> pq;
	cin >> N;
	while (N--) {
		int x;
		cin >> x;
		if (x == 0) {
			if (pq.empty()) {
				cout << 0 << endl;
			}
			else {
				int temp = pq.top();
				cout << temp << endl;
				pq.pop();
			}
		}
		else {
			pq.push(x);
		}
	}

	system("pause");
	return 0;
}*/