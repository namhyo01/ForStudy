#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cmath>
#include <set>
#pragma warning(disable:4996)
using namespace std;
/*
int N, M;
int lists[8];

void dfs(int cnt) {
	if (cnt == M) {
		for (int i = 0; i < M; i++) {
			printf("%d ", lists[i]);
		}
		printf("\n");
		return;
	}
	for (int i = 1; i <= N; i++) {
		lists[cnt] = i;
		dfs(cnt + 1);
	}
}

int main() {

	scanf("%d %d", &N, &M);
	dfs(0);
	system("pause");
	return 0;
}*/