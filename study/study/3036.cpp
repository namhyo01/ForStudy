#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cmath>
#include <set>
#pragma warning(disable:4996)
using namespace std;
/*
int N;
int circle[101];

int gcd(int a, int b) {
	return a % b ? gcd(b, a%b) : b;
}

int main() {
	scanf("%d", &N);
	for (int i = 0; i < N; i++) {
		scanf("%d", &circle[i]);
	}
	for (int i = 1; i < N; i++) {
		int div;
		div = gcd(circle[0], circle[i]);
		printf("%d/%d\n", circle[0] / div, circle[i] / div);
	}


	system("pause");
	return 0;
}*/