#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cmath>
#include <set>
#pragma warning(disable:4996)
using namespace std;/*
int N;
int K;

int main() {
	int mul = 1;
	scanf("%d %d", &N, &K);
	for (int i = K + 1; i <= N; i++) {
		mul *= i;
	}
	for (int i = 2; i <= N - K; i++)
		mul /= i;
	printf("%d\n", mul);
	system("pause");
	return 0;
}*/