#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cmath>
#include <set>
#pragma warning(disable:4996)
using namespace std;

/*
int main() {
	int n;
	cin >> n;
	int temp = n;
	for (int i = 2; i*i < n; i++) {
		while (temp%i == 0) {
			cout << i << endl;
			temp /= i;
		}
	}
	if (temp > 1)
		cout << temp << endl;
	system("pause");
	return 0;
}*/