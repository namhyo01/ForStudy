#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cmath>
#include <set>
#include <map>
#include <string>
//#include <bits/stdc++.h>
#pragma warning(disable:4996)
using namespace std;
/*
int n,T;
string a, b;

int main() {
	scanf("%d", &T);
	for (int i = 0; i < T; i++) {
		map<string, int> _map;
		scanf("%d", &n);
		for (int j = 0; j < n; j++) {
			cin >> a >> b;
			_map[b]++;
		}
		long long ret = 1;
		for (auto c : _map) {
			ret *= ((long long)c.second + 1);
		}
		ret--;
		cout << ret << endl;
	}
	system("pause");
	return 0;
}*/