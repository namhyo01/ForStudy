#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cmath>
#include <set>
#pragma warning(disable:4996)
using namespace std;
/*
bool yaksoo(int a, int b) {
	if (b%a == 0) {
		return true;
	}
	return false;
}
bool baesoo(int a, int b) {
	if (a%b == 0) {
		return true;
	}
	return false;
}
int main() {
	int a, b;
	while (1) {
		scanf("%d %d", &a, &b);
		if (a == 0 && b == 0)
			break;
		if (yaksoo(a, b))
			printf("factor\n");
		else if (baesoo(a, b))
			printf("multiple\n");
		else
			printf("neither\n");
	}

	system("pause");
	return 0;
}*/