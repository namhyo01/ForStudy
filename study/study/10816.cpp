#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <string>
#include <string.h>
#include<math.h>
#include <stdio.h>
#include <memory.h>
#include <stack>
#include <queue>
#include <unordered_map>
using namespace std;
/*
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int N;
	int M;
	cin >> N;
	unordered_map<int, int>card;
	
	for (int i = 0; i < N; i++) {
		int temp;
		cin >> temp;
		if (card.count(temp+10000000)!=0) {
			card[temp+10000000] += 1;
		}
		else {
			card[temp+10000000] = 1;
		}
	}
	cin >> M;
	for (int i = 0; i < M; i++) {
		int temp;
		cin >> temp;
		cout << card[temp + 10000000] << " ";
	}
	cout << endl;
	system("pause");
	return 0;
}*/