#include <bits/stdc++.h>

using namespace std;
/*
void ROT13(char * ch) {
	if (*ch == '\0')
		return;
	if (*ch >= 'A'&&*ch <= 'Z') {
		*ch += 13;
		if (*ch > 'Z')
			*ch -= 26;
		ROT13(ch + 1);

	}
	else if(*ch >= 'a'&&*ch <= 'z') {
		*ch += 13;
		if (*ch > 'z')
			*ch -= 26;
		ROT13(ch + 1);
	}
	else {
		ROT13(ch + 1);
	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	string ch;
	getline(cin, ch);
	ROT13(ch);
	cout << ch << endl;
	system("pause");
	return 0;

}*/