#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <string>
#include<math.h>
/*
using namespace std;
//Ax+Bsin(X) = C
int A, B, C;
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> A >> B >> C;
	long double to = (C - B) / (double)A;
	long double des = (C + B)/(double)A;
	long double mid;
	int cnt = 5000;
	while (to < des&&cnt--) {
		mid = (to + des) / 2;
		long double temp = (C - B*sin(mid)) / A;
		if (mid > temp)
			des = mid;
		else if (mid < temp) 
			to = mid;
		
	}
	printf("%.19Lf\n", mid);
	system("pause");
	return 0;
}*/