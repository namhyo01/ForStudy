#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <string>
#include <string.h>
#include<math.h>
#include <stdio.h>
#include <memory.h>
#include <stack>
#include <queue>
#include <unordered_map>
#include <deque>
using namespace std;
/*
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int K;
	cin >> K;
	long long sum = 0;
	stack<int> temp;
	for (int i = 0; i < K; i++) {
		int now;
		cin >> now;
		if (now == 0&&!temp.empty()) {
			temp.pop();
		}else if(now !=0){
			temp.push(now);
		}
			
	}

	while (!temp.empty()) {
		int a = temp.top();
		sum += a;
		temp.pop();
	}
	cout << sum << endl;
	system("pause");
	return 0;
}*/