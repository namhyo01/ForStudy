#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cmath>
#include <set>
#pragma warning(disable:4996)
using namespace std;
/*
int N;
unsigned int A[101];
int max1 = 0;
/*
bool rjaans(int i) {
	for (int j = 0; j < N - 1; j++) {
		if (A[j] % i != A[j + 1] % i)
			return false;
	}
	return true;
}
int main() {
	scanf("%d", &N);
	for (int i = 0; i < N; i++) {
		scanf("%d", &A[i]);
		max1 = max1 < A[i] ? A[i] : max1;
	}
	int temp = 0;
	for (int i = 2; i <= max1; i++) {
		if (rjaans(i))
			printf("%d ", i);
	}
	system("pause");
	return 0;
}*/